# 스마트플레이스 세팅 정보 수집 폼

네이버 스마트플레이스 세팅에 필요한 정보를 **피부관리샵 사장님에게 입력받는 웹 폼**.
스마트플레이스를 직접 세팅하는 시스템이 아니다. 폼으로 모은 데이터를 담당자가 관리자 화면에 옮겨 심는다.

구현: **Google Apps Script** (HtmlService 웹앱 + Drive + Sheets + MailApp)

---

## 디렉터리

```
appsscript-skeleton/          ← 메인. 여기서 작업한다
  00_Config.gs                  상수 · STEPS 배열 · 이미지 용도
  01_WebApp.gs                  doGet · 클라이언트 API · 진행률 · 교차검증
  02_Session.gs                 진입 인증 · 세션 CRUD · 스냅샷 · 복원
  03_SheetSync.gs               템플릿 복사 · 탭 동기화
  04_Upload.gs                  이미지 업로드
  05_Notify.gs                  담당자 메일
  Index.html / Css.html / Js.html
  appsscript.json

appsscript-upload-prototype/  ← 이미지 업로드만 검증한 초기본. 참고용, 수정 불필요

스마트플레이스_세팅_수집시트_피부관리샵_v26.xlsx   ← 산출물 시트 마스터 템플릿
build_sheet.py                                  ← 위 xlsx 생성 스크립트
스마트플레이스_세팅폼_설계정리_v2.md              ← 전체 설계 문서 (긴 편, 먼저 읽을 것)
```

**시트를 고칠 때는 xlsx를 직접 편집하지 말고 `build_sheet.py`를 수정해 재생성한다.**
재생성 후 반드시 `recalc.py`로 수식 오류 0을 확인한다.

---

## 현재 상태

### 동작함
진입 인증 · 이어하기 · 자동저장 · 스텝퍼 · 이미지 업로드 · 검토·제출 · 메일 · 스냅샷/복원

### 비어 있음
**1~9단계 입력 화면.** `basic`만 절반쯤 예시로 있고 나머지는 `StepRenderers.__placeholder`.

### 배포 전 필수
`00_Config.gs` 상단 3개를 채워야 한다.
```javascript
const SESSION_SHEET_ID  = '';   // 비우면 자동 생성
const TEMPLATE_SHEET_ID = '';   // v26 xlsx를 구글 시트로 변환한 ID
const NOTIFY_EMAIL      = '';
```

---

## 절대 어기면 안 되는 설계 결정

이미 논의로 확정된 것들이다. 바꾸려면 먼저 이유를 물을 것.

| 항목 | 결정 |
|---|---|
| 업체 식별 키 | 업체명 + 대표자명 + **휴대폰 뒷 4자리** |
| 진입 | **로그인 없음**. 웹앱 `모든 사용자` 배포 |
| 저장소 | 작성 중 데이터는 **드라이브 `_session.json`**. 시트는 산출물일 뿐 |
| PropertiesService | **쓰지 말 것**. 전체 500KB 제한이라 몇 업체면 터진다 |
| 시술 마스터 | **정가만**. 할인가는 예약상품에서만 |
| 시간 변형 | 별도 행 (등관리 60분 / 90분을 각각 한 줄) |
| 회차권 | **패키지에서만** 생성. 예약상품 판매유형에 `횟수권` 없음 |
| 판매 유형 | 정가 판매 · 첫방문 체험 · 기간한정 할인 · 무료 체험 **4종** |
| 첫방문 이벤트 | 쿠폰 발급 / 예약상품 할인 **상호배타**. 둘 다 쓰면 할인이 이중 적용 |
| 정가 상품 자동 생성 | 6단계에서 **선택한 시술에만**. 전 시술 자동 생성 금지 |
| "모르겠어요" | 값이 아니라 **상태**(`상담필요`)로 기록. 빈칸보다 나쁜 게 거짓 데이터 |
| 복원 | 이전 버전을 덮어쓰지 않고 **새 버전으로** 생성 |
| 이미지 | 클라이언트 리사이즈 필수(2048px). 20MB 원본은 base64로 26.7MB가 되어 불가 |
| 단계 순서 | `STEPS` 배열로만 정의. **하드코딩 금지** (업종 확장 대비) |
| 라우팅 | 번호 아닌 이름 (`#hours`, `/setup/2` ❌) |
| 담당자 기입란 | 앱이 **덮어쓰지 않음**. `03_SheetSync.gs` `TAB_SPEC`에 적힌 칸만 쓴다 |
| 시트 자동 수식 | 회색 칸(`FFE7E6E6`)은 수식·고정값. **덮으면 죽는다.** 노란 칸(`FFFFF2CC`)만 쓴다 |

---

## Apps Script 제약 (확인됨)

| 항목 | 일반 | Workspace |
|---|---|---|
| 메일 수신자 | 100/일 | **1,500/일** |
| 실행 시간 | 6분/실행 | 6분/실행 |
| 동시 실행 | 30/user | 30/user |
| Properties 총량 | 500 KB | 500 KB |

**Workspace 계정 필수.** 일반 계정은 메일 100통이라 알림만으로 찬다.

자동저장은 **10초 디바운스**. 3초는 콜드 스타트(1~3초) 때문에 저장이 계속 밀린다.
시트 동기화는 **단계 완료 시점에만**. 매 저장마다 쓰면 할당량이 빨리 찬다.

---

## 단계 화면 채우는 법

`Js.html` 아래쪽 `StepRenderers`에 추가한다.

```javascript
StepRenderers.hours = function(body, data){
  body.appendChild(el('h2','page-h','영업시간'));
  var card = el('div','card');
  card.appendChild(field('정기 휴무', 'text', data['정기 휴무'], {
    hint: '예) 매주 월요일 / 매월 둘째·넷째 일요일'
  }));
  body.appendChild(card);
};
```

- `field(라벨, 타입, 현재값, 옵션)` → 입력칸 + 글자수 카운터 + 상태 드롭다운
- **라벨을 시트 항목명과 똑같이** 쓸 것. `writeVertical_()`이 B열 항목명으로 매칭한다
- **항목명이 탭 안에서 겹치면 `구분 · 항목` 형태로 쓸 것.**
  08_스마트콜은 `사용 여부`·`안내 멘트`가 분기마다 반복되므로
  `'1번 통화연결 · 사용 여부'`처럼 A열 구분을 앞에 붙여야 한다.
  이름만 보내면 어느 행인지 정해지지 않아 **기록되지 않고 버려진다**
- 표 형식 단계(영업시간·시술메뉴·패키지·예약상품)는 `StepCollectors.<key>`에서 `{rows:[...]}` 반환.
  각 행 배열은 `TAB_SPEC[탭].cols` 순서와 1:1로 맞춘다.
  **자동 수식 열은 cols에 없으므로 배열에서도 빼야 한다** (06_예약상품의 포함 시술·할인율 등)
- 03_영업시간의 정기휴무·명절휴무·연차안내는 표 밖 15~17행이라 `{rows:[...], tail:{...}}`의 `tail`로 보낸다
- 09_이미지는 업로드가 서버에 쌓으므로 `StepCollectors.image`는 `null`을 반환한다.
  `null`은 "이 단계는 서버 값을 유지하라"는 뜻이고, `{}`를 보내면 업로드 목록이 지워진다

각 화면의 UX 요구사항(영업시간 "매일 같아요" 토글, 시술메뉴 복제 버튼·엑셀 붙여넣기, 패키지 미리보기 블록 등)은
**설계 문서 6장에 화면별로 정리돼 있다. 구현 전 반드시 읽을 것.**

---

## 남은 작업

- [ ] 1~9단계 입력 화면 (`basic` 이어서, `hours` → `menu` → `product` 순 권장)
- [ ] 모바일 실기기 검증 (이미지 20~30장 · EXIF 회전 · 하단 버튼 safe-area)
- [ ] `TEMPLATE_SHEET_ID` 연결 후 시트 동기화 실동작 확인
- [ ] 제출 → 메일 → 재제출 시 변경분 표시 확인
- [ ] 엑셀 붙여넣기 파서 (시술메뉴)
- [ ] 교차 검증 채우기 (`collectIssues_()` — 지금은 자리만 있음)

---

## 작업 규칙

- 한국어로 대화한다
- Apps Script는 로컬 테스트가 안 되므로, 코드 수정 후 **`node --check`로 구문만이라도 검증**한다
  (`.gs`를 `.js`로 복사해서 검사)
- **로직을 고쳤으면 `npm run verify`를 돌린다.** `test/` 아래에 파일별 오프라인 테스트가 있다
  (`test_sheetsync.js`·`test_webapp.js`·`test_session.js`·`test_upload.js`·`test_notify.js`, 공용 도구는 `test/testkit.js`).
  시트 동기화만 따로 보려면:
  ```
  node test/test_sheetsync.js 03_SheetSync.gs
  ```
  Google 서비스(`SpreadsheetApp`/`DriveApp`/`MailApp` 등)가 실제로 얽힌 동작(진입 인증·자동저장·이미지 업로드·
  시트 반영·메일 발송·복원)은 로컬 테스트로 못 잡으므로 `QA_CHECKLIST.md`를 따라 배포본에서 직접 확인한다.
- 파일을 새로 만들기 전에 설계 문서에 이미 정해진 게 있는지 확인한다
- 설계와 다른 판단을 하게 되면 먼저 이유를 설명하고 확인받는다
