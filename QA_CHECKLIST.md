# 라이브 QA 체크리스트

`npm run verify`(구문 검사 · 중복 선언 검사 · 오프라인 로직 테스트)는 코드 레벨의 회귀만 잡는다.
Apps Script는 로컬 실행이 안 되므로 `SpreadsheetApp`/`DriveApp`/`MailApp`/`HtmlService`를 실제로 건드리는
동작(진입 인증, 자동저장, 이미지 업로드, 시트 동기화, 메일 발송, 복원)은 **배포된 웹앱에서 사람이 직접** 확인해야 한다.

이 문서는 그 절차다. 코드가 아니라 사람이 따라가는 체크리스트이므로 자동화 대상이 아니다.

---

## 0. 격리 — 운영 데이터를 절대 건드리지 않는다

`00_Config.gs`에는 이미 실제 운영값(`TEMPLATE_SHEET_ID`, `NOTIFY_EMAIL`)이 들어 있고,
`.clasp.json`은 스크립트 ID 하나에 고정돼 있어 clasp에 프로젝트를 여러 개로 나눠 쓰는 옵션(`--project` 같은 것)이 없다.
그래서 격리는 **git/clasp 밖에서, 한 번만** 손으로 준비한다.

1. **Apps Script 프로젝트 사본**: Apps Script 편집기 → 프로젝트 설정 → **파일 → 사본 만들기**로 QA 전용 스크립트 ID를 확보한다.
2. **시트 사본**: `TEMPLATE_SHEET_ID`가 가리키는 시트도 별도로 "사본 만들기"해서 QA 전용 시트 ID를 확보한다.
3. **알림 메일**: `NOTIFY_EMAIL`을 개인/테스트 주소로 바꾼다 (운영 담당자 메일함에 QA 알림이 섞이면 안 된다).
4. **로컬 작업 공간**: 리포 git 워킹트리 **밖**의 임시 폴더에 `.gs`/`.html`/`appsscript.json`을 복사하고,
   거기서 자체 `.clasp.json`(QA 스크립트 ID)과 QA용 `00_Config.gs`(QA 시트 ID + 테스트 메일)를 두고 `clasp push -f`.
   → 리포의 운영용 `.clasp.json`/`00_Config.gs`는 이 과정에서 전혀 건드리지 않는다. 되돌릴 것도 없다.
5. `/dev` 테스트 배포 URL(`DEPLOY.md`의 "테스트 배포" 참고)만으로는 격리가 안 된다 — 그건 어느 스크립트 프로젝트의
   `/dev`인지가 중요하다. 위 1~4를 먼저 해서 QA 전용 프로젝트를 만든 다음에야 그 프로젝트의 `/dev`가 안전하다.

QA가 끝나면 QA 전용 스크립트/시트는 남겨두거나 지워도 된다 — 운영 쪽엔 영향 없다.

---

## 1. 진입 · 인증

- [ ] 신규 업체: 상호명·대표자명·휴대폰 뒷4자리 입력 → 새 세션 생성, 1단계(`basic`)로 진입
- [ ] 같은 3개 값으로 재진입 → 같은 세션 로드, 이전 입력값·진행 단계 그대로 복원
- [ ] 상호명 또는 대표자명 빈 값 → 에러 메시지, 세션 생성 안 됨
- [ ] 휴대폰 뒷자리에 4자리 숫자가 아닌 값(문자·3자리·5자리) → 에러 메시지
- [ ] 상호명·대표자명은 같고 뒷4자리만 다르게 입력 → **다른 세션**으로 취급됨(새로 시작), 기존 세션 덮어쓰지 않음
    (설계상 의도된 동작 — `makeSettingId_`가 뒷4자리까지 키에 포함하므로)

## 2. 자동저장 · 세션 이어하기

- [ ] 한 필드를 수정하고 10초 이상 대기 → 저장됨 (새로고침 후 값 유지 확인)
- [ ] 필드를 빠르게 여러 번 고치는 동안에는 저장이 계속 미뤄지고, 입력을 멈춘 뒤 10초 후 딱 한 번만 저장되는지
    (매 키 입력마다 저장 요청이 나가면 안 됨 — 할당량 문제)
- [ ] 입력 중 브라우저를 닫거나 뒤로가기 → 재진입 시 직전 저장 시점까지 복원
- [ ] 스텝퍼로 다른 단계 이동 → `currentStep` 갱신, 이후 재진입 시 그 단계에서 이어서 시작

## 3. 이미지 업로드

- [ ] 사진 20~30장 연속 업로드 — 끊기지 않고 전부 올라가는지, 서버 처리 시간이 비정상적으로 늘어나지 않는지
- [ ] 세로(초상)·가로(풍경) 사진 섞어서 업로드 — EXIF 회전이 반영된 채로 저장되는지 (회전 안 된 채로 올라가면 버그)
- [ ] `MAX_UPLOAD_MB`(8MB)를 초과하는 파일 강제로 올리기 → 서버에서 거부, 에러 메시지 노출
    (클라이언트 리사이즈가 실패한 경우를 가정한 방어선)
- [ ] 실제 모바일 기기(iOS/Android)에서 하단 액션 버튼이 안전 영역(홈 인디케이터/제스처 바)에 안 가려지는지
- [ ] 업로드 중 화면이 꺼지거나 앱을 전환했다가 돌아왔을 때 큐가 깨지지 않는지
- [ ] 대표 사진 지정 → 같은 용도 내 기존 대표가 해제되는지 (`apiUpdateImageMeta`의 용도별 단일 대표 규칙)
- [ ] 이미지 삭제 → 목록에서 즉시 사라지고, Drive에서도 휴지통으로 이동하는지

## 4. 시트 동기화 (QA 시트에서만 확인 — 절대 운영 시트로 하지 않는다)

- [ ] 한 단계를 완료 처리 → 해당 탭에 `TAB_SPEC`에 선언된 열만 채워지는지
- [ ] `00_진행현황`, `10_변경이력` 등 `PROTECTED_TABS`는 앱이 아예 건드리지 않는지 (수식·서식 그대로)
- [ ] 각 탭의 `비고` 열은 담당자가 적어둔 메모가 재제출 후에도 그대로 남아있는지
- [ ] 06_예약상품의 E(포함 시술)·I(할인율) 같은 수식 칸이 값 갱신 후에도 수식으로 살아있는지 (덮어써지면 안 됨)
- [ ] 같은 세션으로 두 번째 시트 동기화 시 새 시트를 또 만들지 않고 기존 시트를 재사용하는지 (`sheetId` 재사용)

## 5. 제출 → 메일 → 재제출

- [ ] 첫 제출 → `[신규]` 제목 메일 발송, 본문에 완성도·상담필요/주의 목록이 정확히 반영
- [ ] 몇 개 필드를 고친 뒤 재제출 → `[수정]` 또는 `[변경요청]`(제출 후 상태가 `완료`였던 경우) 제목,
    본문의 "■ 변경" 섹션에 실제로 바뀐 필드만 정확히 나열되는지 (`before → after`)
- [ ] 재제출 시 `status` 전이가 설계대로인지: `완료` → `변경요청`, 그 외에는 기존 상태 유지 또는 `접수`
- [ ] 필수 단계가 하나라도 안 끝났으면 제출 버튼이 막히는지 (`canSubmit=false`)

## 6. 버전 복원

- [ ] 이전 버전 목록에서 하나를 골라 복원 → **새 버전 번호**로 생성됨 (기존 버전을 덮어쓰지 않음)
- [ ] 복원 후 `status`는 복원 시점의 **현재** 상태를 유지 (불러온 스냅샷의 옛 상태로 되돌아가지 않음)
- [ ] 복원 이력(`restoredFrom`)이 남는지, 이전 버전들이 여전히 목록에 남아 있는지

## 7. (선택, 우선순위 낮음) 동시 편집

- [ ] 같은 세션을 두 탭에서 거의 동시에 저장 — `LockService`로 인해 데이터가 깨지지 않는지 확인
